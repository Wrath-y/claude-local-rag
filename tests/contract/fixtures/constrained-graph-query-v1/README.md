# Constrained Graph Query v1 fixtures

These fixtures describe deterministic, namespace-scoped graph-query requests.
The consumer transcript uses an explicit snapshot version for reproducibility;
the provider also supports omitted versions through the active pointer.
