# Graph service operability contract fixtures

Deterministic `/v1` payloads for host compatibility discovery, explicit
idempotent rebuilds, durable task polling, restart recovery, atomic failure
preservation, and reimport. `manifest.json` SHA-256 digests every JSON file.
